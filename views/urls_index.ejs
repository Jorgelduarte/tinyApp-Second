<!DOCTYPE html>
<html lang="en">
<head>
  <style>
      body { 
          background-color: black;
          color: white;
          margin: 120px 
      } 
      </style>
</head>
<body>
  <header>
      <% include partials/_header %>
  </header>
     file: urls_index.ejs
      
       <% for (key in urls) { %>
        <ul>
            <li><%= key %> : <%=urls[key]%></li>
        </ul>
        <form method="POST" action="/urls/<%= key %>/delete"><button type="submit">Delete 🗑 </button></form>
       <% } %>
       
       <a href= >Shortening a new URL</an>
</body>
</html>