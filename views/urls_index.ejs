<!DOCTYPE html>
<html lang="en">
    <head>
            <link rel="stylesheet" href="styles.css">
        <style>
            body { 
                margin: 120px;
                font-size: 20px;
            } 
        </style>
    </head>
    <body>
        <header>
            <% include partials/_header %>
        </header>
        
        file: urls_index.ejs

        <% for (key in urls) { %>
        <ul>
            <li><%= key %> : <%=urls[key]%>
                <form style="display: inline;" method="POST" action="/urls/<%= key %>/delete">
                    <button type="submit" >Delete 🗑 </button>
                </form>
                <a href="/urls/<%= key %>" > edit </a>
            </li>
        </ul>
        <% } %>
        <div>
            <a href=http://localhost:8080/urls/new >Shortening a new URL</an>
        </div>
    </body>
</html>